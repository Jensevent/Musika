name: Development

on:
  push:
    branches: [dev]


jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        test:
          - "A"
          - "B"
    
    steps:
      - name: Test
      -  run: echo ${{matrix.test}}

