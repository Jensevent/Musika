name: "Deploy test"
# shell: bash
description: "test a deploy function"
inputs:
  foo:
    required: true
    decription: "ABC"

runs:
  using: "composite"
  steps:
    - name: Clone repo to runner
      uses: actions/checkout@v3

    - name: Install dependencies
      shell: bash 
      run :  dotnet restore ${{inputs.foo}}